{
  "name": "react-instruments",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun run core/transformer.ts && bun build demo-script-transformed.ts --outdir dist",
    "dev": "bun run --hot server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@babel/core": "^7.25.2",
    "@types/babel__core": "^7.20.5"
  }
}
